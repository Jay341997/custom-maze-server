import { Mat } from '.';
export declare class Graph {
    private needsBackpropagation;
    private readonly backpropagationStack;
    constructor();
    memorizeOperationSequence(isMemorizing?: boolean): void;
    isMemorizingSequence(): boolean;
    forgetCurrentSequence(): void;
    backward(): void;
    rowPluck(m: Mat, rowIndex: number): Mat;
    private addRowPluckToBackpropagationStack;
    gauss(m: Mat, std: Mat): Mat;
    tanh(m: Mat): Mat;
    private addTanhToBackpropagationStack;
    sig(m: Mat): Mat;
    private addSigmoidToBackpropagationStack;
    relu(m: Mat): Mat;
    private addReluToBackpropagationStack;
    add(m1: Mat, m2: Mat): Mat;
    private addAdditionToBackpropagationStack;
    mul(m1: Mat, m2: Mat): Mat;
    private addMultiplyToBackpropagationStack;
    dot(m1: Mat, m2: Mat): Mat;
    private addDotToBackpropagationStack;
    eltmul(m1: Mat, m2: Mat): Mat;
    private addEltmulToBackpropagationStack;
}
