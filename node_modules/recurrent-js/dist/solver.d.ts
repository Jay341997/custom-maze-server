import { RNNModel } from './rnn/rnn-model';
export declare class Solver {
    protected readonly decayRate: number;
    protected readonly smoothEps: number;
    protected readonly stepCache: {};
    protected stepTotalNumber: number;
    protected stepNumberOfClippings: number;
    constructor(decayRate?: number, smoothEps?: number);
    private reset;
    step(model: RNNModel, stepSize: number, l2Regularization: number, clippingValue: number): {
        'ratioClipped': number;
    };
    private iterateModelLayer;
    private RMSprop;
    private gradientClipping;
    private update;
    private resetGradients;
}
