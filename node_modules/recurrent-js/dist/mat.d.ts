import { Assertable } from './utils/assertable';
export declare class Mat extends Assertable {
    readonly rows: number;
    readonly cols: number;
    private readonly _length;
    readonly w: Array<number> | Float64Array;
    readonly dw: Array<number> | Float64Array;
    constructor(rows: number, cols: number);
    get(row: number, col: number): number;
    set(row: number, col: number, v: number): void;
    protected getIndexBy(row: number, col: number): number;
    setFrom(arr: Array<number> | Float64Array): void;
    setColumn(m: Mat, colIndex: number): void;
    equals(m: Mat): boolean;
    static toJSON(m: Mat | any): {
        rows: any;
        cols: any;
        w: any;
    };
    static fromJSON(json: {
        rows: any;
        n?: any;
        cols: any;
        d?: any;
        w: any;
    }): Mat;
    update(alpha: number): void;
}
