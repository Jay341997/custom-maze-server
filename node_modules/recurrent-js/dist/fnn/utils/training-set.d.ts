import { Sample } from "./sample";
export declare class TrainingSet {
    private samples;
    setSamples(samples: Array<Sample>): void;
    length(): number;
    getInputForSample(i: number): Array<number>;
    getExpectedOutputForSample(i: number): Array<number>;
}
