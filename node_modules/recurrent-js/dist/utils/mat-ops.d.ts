import { Mat } from '..';
import { Assertable } from './assertable';
export declare class MatOps extends Assertable {
    static rowPluck(m: Mat, rowIndex: number): Mat;
    static getRowPluckBackprop(m: Mat, rowIndex: number, out: Mat): Function;
    static gauss(m: Mat, std: Mat): Mat;
    static tanh(m: Mat): Mat;
    static getTanhBackprop(m: Mat, out: Mat): Function;
    static sig(m: Mat): Mat;
    private static sigmoid;
    static getSigmoidBackprop(m: Mat, out: Mat): Function;
    static relu(m: Mat): Mat;
    static getReluBackprop(m: Mat, out: Mat): Function;
    static add(m1: Mat, m2: Mat): Mat;
    static getAddBackprop(m1: Mat, m2: Mat, out: Mat): Function;
    static mul(m1: Mat, m2: Mat): Mat;
    static getMulBackprop(m1: Mat, m2: Mat, out: Mat): Function;
    static dot(m1: Mat, m2: Mat): Mat;
    static getDotBackprop(m1: Mat, m2: Mat, out: Mat): Function;
    static eltmul(m1: Mat, m2: Mat): Mat;
    static getEltmulBackprop(m1: Mat, m2: Mat, out: Mat): Function;
}
