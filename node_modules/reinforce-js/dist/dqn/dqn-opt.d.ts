import { Opt } from './../.';
export declare class DQNOpt extends Opt {
    protected trainingMode: boolean;
    protected numberOfHiddenUnits: Array<number>;
    protected epsilonMax: number;
    protected epsilonMin: number;
    protected epsilonDecayPeriod: number;
    protected epsilon: number;
    protected gamma: number;
    protected alpha: number;
    protected experienceSize: number;
    protected doLossClipping: boolean;
    protected lossClamp: number;
    protected doRewardClipping: boolean;
    protected rewardClamp: number;
    protected keepExperienceInterval: number;
    protected replaySteps: number;
    setNumberOfHiddenUnits(numberOfHiddenUnits: Array<number>): void;
    setEpsilonDecay(epsilonMax: number, epsilonMin: number, epsilonDecayPeriod: number): void;
    setEpsilon(epsilon: number): void;
    setGamma(gamma: number): void;
    setAlpha(alpha: number): void;
    setLossClipping(doLossClipping: boolean): void;
    setLossClamp(lossClamp: number): void;
    setRewardClipping(doRewardClipping: boolean): void;
    setRewardClamp(rewardClamp: number): void;
    setTrainingMode(trainingMode: boolean): void;
    setExperienceSize(experienceSize: number): void;
    setReplayInterval(keepExperienceInterval: number): void;
    setReplaySteps(replaySteps: number): void;
}
